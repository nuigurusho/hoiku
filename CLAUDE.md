# 開発ルール

このリポジトリで作業するときの決まりごと。Claude も人も、ここに書いてあることを基本とする。

**作業を始める前に [`notes/HANDOVER.md`](notes/HANDOVER.md) を読むこと。**
いまの状態・積み残し・アプリ化の検討結果・当日の運用・検証のやり方がまとまっている。

## 1. 作業の流れ(基本)

**このリポジトリは main に直接 push する。PR は作らない。**

```
# 直す
git commit
git push origin main
```

- main へ push すると `.github/workflows/deploy-pages.yml` が走り、**そのまま本番(GitHub Pages)に公開される**。
  つまり **push = 公開**。作りかけを入れない・動くところまで確認してから push する。
- ブランチを切って作業してもよいが、**最後は main に入れて push するところまでやる**
  (PR を作って止めない)。
- コミットメッセージに「なにが起きていたか」「なぜそうなったか」「どう直したか」を書く。
  1つのコミットに複数のテーマを混ぜず、テーマごとに分ける。
- マージしたブランチは消してよい。

## 2. 機能を消すときは、その機能ぶんだけ消す

モードや画面を削除するときに、**残るモードでも使っている共通処理まで巻きこんで消さない。**

実例(`games/adventure.html`):
「みるだけ(オート)」を消したとき、開始時に

```js
$("touchpad").classList.toggle("hidden", !!mode.auto);
```

も一緒に消してしまった。これは**タッチ操作ボタンを表示する唯一の行**だったため、
残った「れんしゅう」「ガチンコ」でもボタンが出ず、タブレットでキャラを動かせなくなった。

消す前に、その行が **消すモード専用か / 全モード共通か** を確かめる。
`mode.xxx` の条件がついていても、条件の外側(表示・初期化)まで消していないか見る。

## 3. 直したら、実機と同じ条件で最後まで動かす

- **タッチ操作は必ずタッチありの状態で確認する。** PC のキーボードだけだと今回のような不具合を見のがす。
- 遊ぶ人はタブレット・スマホの子ども。**画面のボタンだけで最初から最後まで遊べるか**を通しで確認する。
- ローカル確認は `./serve.sh`(または `serve.bat`)→ `http://localhost:8765`。
  `file://` だと IndexedDB や画像の読みこみが本番と違う動きをする。

## 4. 共有ファイルを変えたら `?v=` を上げる

`js/core.js`・`js/rig.js`・`js/meet.js`・`js/editors.js`・`css/style.css` を変えたら、
**それを読んでいる全 HTML の `?v=` を同じ番号に上げる**(古いキャッシュが残らないように)。

```bash
grep -rn "core.js?v=" *.html games/*.html   # 数字がそろっているか確認
```

PWAの本番キャッシュ番号と公開ファイル一覧は、GitHub Actionsが `tools/build-pwa.mjs` を使って
コミットIDから自動生成する。`sw.js` の番号を手で直す必要はない。
ローカルで追加ファイルをオフライン確認するときだけ、サーバー起動前に次を実行する。

```bash
PWA_BUILD_ID=local-test node tools/build-pwa.mjs
```

Windows PowerShellでは `$env:PWA_BUILD_ID="local-test"; node tools/build-pwa.mjs`。

## 5. 書きかた

- **ゲーム全体で、操作は見た目で分かるようにする。** 子ども向け画面では、絵・配置・形・動きで伝え、
  よほど必要な説明や大人向けの情報を除いて、小さい補足文や重複説明を置かない。
- **ひらがな中心にするのは、画面に出る子供向けの文言だけ**(対象は1〜6歳)。
  漢字を使うときはふりがな相当のやさしい言い方にする。
- **それ以外は通常の日本語(漢字混じり)で書く。** 対象は大人なので、読みやすさを優先する。
  - ひらがな中心にするもの … ゲーム画面のボタン・見出し・メッセージ、子供に見せる説明
  - 通常の日本語で書くもの … コミットメッセージ、PR の本文、README、コード内コメント、
    設計メモ、チャットでの説明や提案
- コミットメッセージも、この README・コード内コメントと同じく日本語で書く。
- 外部ライブラリ・ネット接続に依存する書きかたをしない(オフラインで動くことが前提)。
