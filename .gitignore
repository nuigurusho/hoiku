# デザイン提案のキャンバス(エディタごと書き出すので 2MB 近くある)。
# design/<名前>/ の build.py と *.dc.html からいつでも作り直せるので、
# 書き出したものはリポジトリに入れない。
design/*/*.html
!design/*/*.dc.html

# Codexの添付・PDF目視確認で作る一時ファイル。園児名を含む原稿を誤って公開しない。
.codex-remote-attachments/
tmp/
